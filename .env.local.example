ANTHROPIC_API_KEY=sk-ant-...

# Optional — when set, uploads use Vercel Blob (private) instead of local disk.
# BLOB_READ_WRITE_TOKEN=vercel_blob_...

# Optional explicit override; otherwise auto: vercel-blob if BLOB_READ_WRITE_TOKEN is set, else local-fs.
# STORAGE_BACKEND=local-fs
